# Data Directory

Place the preprocessed arXiv dataset here before running the app.

Required file:

```text
data/arxiv_preprocessed.csv
```

The raw arXiv metadata snapshot is optional for running the Flask app.
